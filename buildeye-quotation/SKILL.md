---
name: buildeye-quotation
description: מחולל הצעות מחיר בילדאיי (BuildEye Quotation Generator). השתמש בסקיל זה תמיד כאשר המשתמש מבקש ליצור, למלא, להכין, או להפיק הצעת מחיר לפרויקט סריקה ומידול — בין אם אמר "מלא הצעת מחיר", "צור הצעת מחיר", "הצעת מחיר חדשה", "new quotation", "new quote", "create a quote", "fill a quotation", "הכן הצעה ללקוח", "הצעה ל[שם לקוח]", "אני צריך הצעת מחיר", "תכין לי הצעה" או כל ניסוח דומה. הסקיל שואל את המשתמש שאלות מודרכות בעברית ומייצר קובץ DOCX מוכן למסירה בהתבסס על טמפלייט הצעות המחיר הסטנדרטי של בילדאיי.
---

# מחולל הצעות מחיר — בילדאיי

הסקיל יוצר הצעת מחיר ממולאת (DOCX) על בסיס טמפלייט בילדאיי הסטנדרטי.
מספר ההצעה מנוהל אוטומטית על ידי הסקריפט (counter file בתיקיית הפלט).

## כלל חשוב לשאלות

- **שאלות עם אפשרויות קבועות** (בחירה מרשימה) → השתמש ב-AskUserQuestion
- **שאלות עם טקסט חופשי** (שם, כותרת, תיאור, הנחות) → שאל בצ'אט רגיל, המשתמש מקליד את התשובה

---

## נתיבים חשובים

**שלב 0 — בקש גישה לתיקיות המשתמש (לפני כל דבר אחר):**

השתמש ב-`request_cowork_directory` עם שני הנתיבים הבאים:
1. `C:\Users\1\BuildEye\MGMT - Documents\הצעות מחיר`
2. `C:\Users\1\BuildEye\MGMT - Documents\CLAUDE\BuildEye Skills\buildeye-quotation\scripts`

לאחר שהתיקיות עלו לסשן, הרץ:

```bash
TEMPLATE=$(find /sessions -name "buildeye_quotation.docx" 2>/dev/null | head -1)
COUNTER_DIR=$(dirname "$TEMPLATE")
OUTPUT_DIR="$COUNTER_DIR/2026/SKILL"
SCRIPT=$(find /sessions -name "generate_quotation.py" 2>/dev/null | grep -v "\.claude" | head -1)
```

---

## שלב 1: שאלות טקסט חופשי (בצ'אט)

שאל את השאלות הבאות **בצ'אט רגיל** ביחד, וחכה לתשובת המשתמש:

1. **"שם הלקוח?"** (לדוגמה: שלומי כוכבי)
2. **"תפקיד הלקוח?"** (לדוגמה: מנהל פרויקטים)
3. **"שם החברה?"** (לדוגמה: מפטגון)
4. **"מה כותרת ההצעה (שורת הנדון)?"** — לדוגמה: הצעת מחיר לסריקה ומידול מבנה אחוזת בית תל אביב
5. **"מה תיאור הפרויקט לגוף המסמך?"** (ממלא: 'הנני מתכבד להגיש הצעה עבור ___') — אפשר לרשום "זהה לכותרת"

---

## שלב 2: בחירת תוצרים (AskUserQuestion — 2 קריאות)

### קריאה ראשונה — 1 שאלה:

**"אילו תוצרים עיקריים לכלול?"** (header: "תוצרים", multiSelect: true)
- label: "סריקה (ענן נקודות)", description: "ענן נקודות פנים וחזיתות, פורמטים RCP + LGS"
- label: "מודל תלת ממדי (Revit)", description: "מידול BIM מלא לפי Golden Standard בילדאיי"
- label: "צפיין בילדאיי Spheres", description: "תוסף הטמעת תמונות 360 לרוויט — ללא תוספת עלות"
- label: "תוכנית מדידה (AutoCAD)", description: "תוכנית רצפה עם פתחים, מחיצות, גבהים"

### קריאה שנייה — 1 שאלה:

**"תוצרים נוספים?"** (header: "תוצרים נוספים", multiSelect: true)
- label: "חזיתות", description: "תוכנית לכל חזית: גבהי מפלסים, פתחים, אלומיניום"
- label: "חתכים", description: "חתכים לפי סימון המזמין"
- label: "סיור וירטואלי תלת-ממדי", description: "לינק, מדידה עצמאית, שמירה בענן לשנה"

---

## שלב 3: שאלת טקסט חופשי — הנחות תמחור (בצ'אט)

שאל בצ'אט:

6. **"מה הנחות התמחור? (שטח לסריקה, כמות קומות — לדוגמה: שטח סריקה 2,000 מ\"ר, 5 קומות) — אפשר להשאיר ריק"**

---

## שלב 4: שאלות בחירה — תנאים (AskUserQuestion)

### אם המשתמש בחר "תוכנית מדידה" — שאל 3 שאלות בקריאה אחת:

**שאלה א׳ — קנ"מ** (header: "קנ\"מ")
"מה קנה המידה לתוכניות המדידה?"
- label: "1:50", description: "קנה מידה גדול"
- label: "1:100", description: "קנה מידה סטנדרטי"
- label: "1:250", description: "קנה מידה קטן"

**שאלה ב׳ — ימי אספקה** (header: "ימי אספקה")
"כמה ימי עסקים לאספקת תוצרי המידול?"
- label: "10 ימי עסקים", description: "פרויקטים קטנים"
- label: "15 ימי עסקים", description: "סטנדרטי"
- label: "20 ימי עסקים", description: "פרויקטים גדולים"
- label: "אחר — הקלד מספר", description: "כתוב את מספר הימים בתיבת 'אחר'"

**שאלה ג׳ — חישוב שטח** (header: "חישוב שטח")
"חישוב השטח הכולל לתשלום יכלול את:"
- label: "קירות המעטפת (ברוטו)", description: "כולל עובי קירות חיצוניים"
- label: "פנים המעטפת בלבד", description: "שטח נטו"

### אם המשתמש לא בחר "תוכנית מדידה" — שאל 2 שאלות בקריאה אחת:

**שאלות ב׳ + ג׳ בלבד** (ימי אספקה + חישוב שטח, בלי קנ"מ).

---

## שלב 5: תנאי תשלום (AskUserQuestion — קריאה נפרדת)

**"מה תנאי התשלום?"** (header: "תנאי תשלום")
- label: "שוטף +30", description: "תשלום שוטף בתוספת 30 יום"
- label: "שוטף +45", description: "תשלום שוטף בתוספת 45 יום"
- label: "30% עם קבלת הזמנה חתומה, 70% עם הגשת התוצרים", description: "30% עם קבלת ההזמנה החתומה, 70% עם הגשת התוצרים"
- label: "40% לאחר סיום העבודה בשטח, 60% עם הגשת התוצרים", description: "40% לאחר סיום העבודה בשטח, 60% עם הגשת התוצרים"

_(שוטף + 60 / שוטף + 90 — דרך תיבת 'אחר')_

---

## שלב 6: הרץ את הסקריפט

לפני הרצה — המר תשובות:
- **תוצרים**: חבר רשימות שלב 2 (קריאה 1 + קריאה 2) לרשימה מופרדת בפסיקים
- **תיאור פרויקט**: אם המשתמש אמר "זהה לכותרת" — השתמש בתשובת שאלה 4
- **הנחות תמחור**: אם ריק / "השאר ריק" — העבר `""`
- **קנ"מ**: אם אין "תוכנית מדידה" — העבר `""`
- **תנאי תשלום**: העבר את הייבל כמות שהוא — כל 4 האפשרויות הן הטקסט הסופי שיופיע במסמך

```bash
python "$SCRIPT" \
  --template "$TEMPLATE" \
  --output-dir "$OUTPUT_DIR" \
  --counter-dir "$COUNTER_DIR" \
  --client-name "שם הלקוח" \
  --client-position "תפקיד" \
  --company-name "שם החברה" \
  --title "הצעת מחיר לסריקה ומידול [פרויקט]" \
  --project "תיאור לגוף המסמך" \
  --services "סריקה,מודל,חזיתות" \
  --pricing-notes "שטח סריקה 2,000 מ\"ר, 5 קומות" \
  --scale "1:100" \
  --delivery-days "15" \
  --area-basis "פנים המעטפת בלבד" \
  --payment-terms "שוטף +30"
```

הסקריפט ידפיס:
```
SUCCESS: /path/to/321_חברה - הצעת מחיר לסריקה ומידול פרויקט.docx
QUOTE_NUMBER: 321
```

**הערות חשובות:**
- מספר ההצעה מנוהל אוטומטית ב-`quote_counter.json` בנתיב `$COUNTER_DIR`
- שם הקובץ כולל אוטומטית את מספר ההצעה ושם החברה
- שדה "הצעה מס':" בכותרת המסמך ימולא אוטומטית
- אם נבחר "תוכנית מדידה" ללא "מודל תלת ממדי" — שורת Revit תוסר, "מידול"→"מיפוי", "ימודל"→"ימופה", ושורת "המרה" תוסר אוטומטית
- עמודת "#" ממוספרת מחדש אוטומטית לאחר הסרת שורות
- **פונטים:** טקסט שמוסיף הסקריפט — Arial (Body CS) גודל 11. שורת הנדון (כותרת ההצעה) — Heading 1, Times New Roman 18, Bold, Underline

---

## שלב 7: הצג את הקובץ

```
[פתח את הצעת המחיר](computer://C:\Users\1\BuildEye\MGMT - Documents\הצעות מחיר\2026\SKILL\[שם_הקובץ])
```

הזכר למשתמש: **עמודת המחיר ריקה — יש למלא ידנית בקובץ**.

---

## הערות לשגיאות

- אם הטמפלייט פגום — הסקריפט יידע לדווח
- נתיב הסקריפט: `C:\Users\1\BuildEye\MGMT - Documents\CLAUDE\BuildEye Skills\buildeye-quotation\scripts\generate_quotation.py`
- counter file נשמר ב: `C:\Users\1\BuildEye\MGMT - Documents\הצעות מחיר\quote_counter.json`
- תיקיית פלט: `C:\Users\1\BuildEye\MGMT - Documents\הצעות מחיר\2026\SKILL` (נוצרת אוטומטית אם לא קיימת)
